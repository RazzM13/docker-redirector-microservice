{
  "name": "redirect_microservice",
  "version": "0.1.0",
  "description": "A simplistic Docker based URL shortner / redirector microservice.",
  "main": "src/index.js",
  "engines": {
    "node": ">= 0.10.0"
  },
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "build": "docker-compose build",
    "start": "docker-compose up",
    "dev": "docker-compose -f compose.yaml -f compose.dev.yaml up --build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RazzM13/docker-redirector-microservice"
  },
  "author": "RazzM13",
  "license": "MIT"
}
